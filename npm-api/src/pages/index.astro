---
const response =await fetch('https://jsonplaceholder.typicode.com/posts/1/comments')
const data =await response.json()
interface responseData{
	postId:number
	id:number
	name:string
	email:string
	body:string
}//for item // response data 
---

<html lang="en">
	<head>
		<meta charset="utf-8" />
		<link rel="icon" type="image/svg+xml" href="/favicon.svg" />
		<meta name="viewport" content="width=device-width" />
		<meta name="generator" content={Astro.generator} />
		<title>Astro</title>
	</head>
	<body>
		<h1 class="text-4x1 container mx-auto">Astro API</h1>
		{
		data.map((item:responseData)=>(//item is set to any hence we have to create an interface
			<ul>
				<li>Post Name:{item.name}</li>
				<li>Email:{item.email}</li>
				<li>Text:{item.body}</li>

			</ul>
		))
		}
		<!-- This is the way to fetch data in astro -->
		<!-- We are trying to fetch data from json placeholder -->
		<!-- We are taking a json dummy data, and then we are creating an interface named response Data -->
		<!-- Response Data is an interface, then we are mapping responseData to item-->
		<!--So we are trying to print postname,email and text data from data -->
	</body>
</html>


